# Home-only dependency for `manage.py publish_game_data` (uploads game data to
# Cloudflare R2 via the S3 API). NOT needed by the Render web service. Install at
# home alongside the weekly refresh: pip install -r requirements-publish.txt
boto3

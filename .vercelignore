# Keep the Vercel upload lean. NOTE: do NOT ignore backend/trivia/data — the build
# (npm run copy:data) reads it to publish /data/. Only the heavy, irrelevant parts:
backend/venv
backend/staticfiles
backend/media
backend/db.sqlite3
backend/**/__pycache__
multiplayer_server
docs
